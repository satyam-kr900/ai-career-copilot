# Build output
/.next/
/out/
/build

# Dependencies
/node_modules

# Database (SQLite local only — use Vercel Postgres or Neon in production)
/prisma/dev.db
/prisma/dev.db-journal

# Local env — NEVER commit real keys
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# OS
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript build cache
tsconfig.tsbuildinfo
.vercel
